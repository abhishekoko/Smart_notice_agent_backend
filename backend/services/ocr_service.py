# backend/services/ocr_service.py

def extract_text_from_image(image_path: str) -> str:
    """
    OCR is completely disabled for Render.
    This stub keeps the import working without errors.
    Always returns an empty string.
    """
    return ""
